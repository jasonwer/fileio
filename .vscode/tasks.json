{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build fileio.c",
            "type": "shell",
            "command": "gcc -Wall -g fileio.c -o fileio",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}